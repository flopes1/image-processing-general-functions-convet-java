package com.poli.model.morphology.common;

public enum EnumMorphologyOperation
{
    // XAND == XNOR
    AND, OR, XOR, XAND, DILATION, EROSION, OPENNING, CLOSING, EXTRACTION, REDUCTION, FILLING, NOT

}
